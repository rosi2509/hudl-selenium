# Application configuration
BASE_URL=https://www.hudl.com
LOGIN_PATH=/login
IMPLICIT_TIMEOUT_MS=0
EXPLICIT_TIMEOUT_MS=10000
HEADLESS=false

# Credentials (replace these in your own .env file, never commit them)
HUDL_EMAIL=your_email@example.com
HUDL_PASSWORD=your_password
